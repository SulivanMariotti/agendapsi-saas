# AgendaPsi — Changelog (chat atual)

Data: **2026-03-03**

Este changelog resume o que foi implementado/ajustado no chat (para referência rápida).

## Portal do Paciente
- Fix T08/T09:
  - Anotações usando `patientApp` (Auth isolado)
  - Toggle lembretes refletindo valor efetivo
- Segurança em APIs:
  - `enforceSameOrigin` e `rateLimit` aplicados corretamente em notes/library/access-code
- Settings por tenant:
  - `tenants/{tenantId}/settings/patientPortal` (termo + versão + flags)
- Dev aid:
  - Badge DEV exibindo `tenantId` ativo (opcional)
- Tenant suspension:
  - bloqueio real com retorno 403 `TENANT_SUSPENDED` + UX amigável

## Super Admin SaaS
- Tenants: criar/listar/buscar/suspender/reativar
- Vincular Owner por email/uid existente no Auth
- Auditoria mínima em `audit_logs`

## WhatsApp
- Templates WhatsApp por tenant (Admin CRUD)
- Profissional: select de template + placeholders `{nome} {data} {hora}`
- Fix de menu para exibir “AgendaPsi → Templates WhatsApp”

